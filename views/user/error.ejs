<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - SIGMA UNAND</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-red-50 to-pink-100 min-h-screen flex items-center justify-center">
  <div class="max-w-md w-full mx-4">
    <div class="bg-white rounded-xl shadow-lg p-8 text-center">
      <div class="mb-6">
        <div class="bg-red-100 w-20 h-20 rounded-full flex items-center justify-center mx-auto">
          <i class="fas fa-exclamation-triangle text-3xl text-red-600"></i>
        </div>  
      </div>
      <h1 class="text-2xl font-bold text-gray-800 mb-4">Oops! Terjadi Kesalahan</h1>
      <p class="text-gray-600 mb-8"><%= message %></p>
      <% if (typeof backUrl !== 'undefined' && backUrl) { %>
        <a href="<%= backUrl %>" class="block w-full bg-blue-600 text-white p-3 rounded-md">Kembali</a>
      <% } %>
      <a href="/forum" class="block w-full bg-gray-200 text-gray-800 p-3 rounded-md">Ke Forum</a>
    </div>
  </div>
</body>
</html>
