<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Laporan Aktivitas - <%= laporan.judul %></title>
    <style>
        body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; color: #333; }
        .container { padding: 40px; }
        h1 { text-align: center; border-bottom: 2px solid #eee; padding-bottom: 10px; margin-bottom: 30px; }
        .info { margin-bottom: 20px; }
        .info span { font-weight: bold; }
        .detail-box { border: 1px solid #ddd; padding: 15px; background-color: #f9f9f9; min-height: 200px; }
        .footer { text-align: center; margin-top: 40px; font-size: 12px; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Laporan Aktivitas</h1>

        <div class="info">
            <p><span>Judul Laporan:</span> <%= laporan.judul %></p>
            <p><span>Tanggal Kegiatan:</span> <%= new Date(laporan.tanggal).toLocaleString('id-ID', { dateStyle: 'full', timeStyle: 'short' }) %></p>
        </div>

        <h3>Detail Kegiatan:</h3>
        <div class="detail-box">
            <p><%= laporan.detail %></p>
        </div>

        <div class="footer">
            Dokumen ini dibuat secara otomatis pada <%= new Date().toLocaleString('id-ID') %>
        </div>
    </div>
</body>
</html>